<UserControl x:Class="ProfiSysTask.UI.Views.DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#E6E6E6">
    <UserControl.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#004A80"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,15"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="RoundActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PillButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007CC3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,10,10,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#007CC3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0" VerticalAlignment="Center"/>

                                <Path x:Name="SortArrow" 
                                      Grid.Column="1" 
                                      Margin="8,0,0,0" 
                                      VerticalAlignment="Center" 
                                      Fill="#007CC3" 
                                      Visibility="Collapsed"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="SortDirection" Value="Ascending">
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortArrow" Property="Data" Value="M 0,5 L 5,0 L 10,5 Z"/>
                            </Trigger>
                            <Trigger Property="SortDirection" Value="Descending">
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortArrow" Property="Data" Value="M 0,0 L 5,5 L 10,0 Z"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#EEEEEE"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F8FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#007CC3"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="HorizontalGridLinesBrush" Value="Transparent"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="#007CC3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="Importuj ponownie .csv" Command="{Binding ImportDataCommand}" Style="{StaticResource SidebarButtonStyle}"/>
            <Button Grid.Row="1" Content="Pobierz ponownie bazę" Command="{Binding LoadDataCommand}" Style="{StaticResource SidebarButtonStyle}"/>
            <Button Grid.Row="2" Content="Eksportuj do plików CSV" Command="{Binding ExportDataCommand}" Style="{StaticResource SidebarButtonStyle}"/>
            <Button Grid.Row="3" Content="Generuj raport dla dokumentu" Command="{Binding GenerateReportCommand}" Style="{StaticResource SidebarButtonStyle}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Margin="0,0,0,2">
                            <TextBlock Text="{TemplateBinding Content}" TextWrapping="Wrap" TextAlignment="Center" Padding="10,10"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Grid.Row="5" Content="Wyczyść bazę danych" Command="{Binding ClearDatabaseCommand}" Style="{StaticResource SidebarButtonStyle}" Background="#990000"/>
            <Button Grid.Row="6" Content="Wróć" Command="{Binding GoBackCommand}" Style="{StaticResource SidebarButtonStyle}" Background="#333333"/>
        </Grid>

        <Grid Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1.8*"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Dokumenty" FontSize="14" Margin="0,0,0,5"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DataGrid Grid.Column="0" ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument}" 
          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
          Style="{StaticResource ModernDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" MinWidth="40"/>
                        <DataGridTextColumn Header="Typ" Binding="{Binding Type}" Width="*"/>
                        <DataGridTextColumn Header="Data" Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" Width="*"/>
                        <DataGridTextColumn Header="Imię" Binding="{Binding FirstName}" Width="*"/>
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding LastName}" Width="*"/>
                        <DataGridTextColumn Header="Miasto" Binding="{Binding City}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Margin="15,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="+" Background="#008A00" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding AddDocumentCommand}"/>
                        <Button Content="Wszystko ▼" Style="{StaticResource PillButtonStyle}" Margin="10,0,0,10"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="✎" Background="#996600" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding EditDocumentCommand}"/>
                        <Button Content="....." Style="{StaticResource PillButtonStyle}" Margin="10,0,0,10"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="-" Background="#990000" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding DeleteDocumentCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="Transparent"/>

            <TextBlock Grid.Row="3" Text="Elementy dokumentu" FontSize="14" Margin="0,10,0,5"/>
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding SelectedDocument.Items}"
          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
          Style="{StaticResource ModernDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Lp." Binding="{Binding Ordinal}" Width="Auto" MinWidth="40"/>
                        <DataGridTextColumn Header="Produkt" Binding="{Binding Product}" Width="2*"/>
                        <DataGridTextColumn Header="Ilość" Binding="{Binding Quantity}" Width="*"/>
                        <DataGridTextColumn Header="Cena" Binding="{Binding Price, StringFormat=\{0:C\}}" Width="*"/>
                        <DataGridTextColumn Header="VAT (%)" Binding="{Binding TaxRate}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Margin="15,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="+" Background="#008A00" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding AddItemCommand}"/>
                        <Button Content="Wszystko ▼" Style="{StaticResource PillButtonStyle}" Margin="10,0,0,10"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="✎" Background="#996600" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding EditItemCommand}"/>
                        <Button Content="....." Style="{StaticResource PillButtonStyle}" Margin="10,0,0,10"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="-" Background="#990000" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding DeleteItemCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>