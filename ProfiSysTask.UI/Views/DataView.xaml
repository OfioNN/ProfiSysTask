<UserControl x:Class="ProfiSysTask.UI.Views.DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#E6E6E6">
    <UserControl.Resources>
        <Style x:Key="SidebarBaseButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,15"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarBlueButtonStyle" TargetType="Button" BasedOn="{StaticResource SidebarBaseButtonStyle}">
            <Setter Property="Background" Value="#004A80"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#003055"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SidebarRedButtonStyle" TargetType="Button" BasedOn="{StaticResource SidebarBaseButtonStyle}">
            <Setter Property="Background" Value="#990000"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#660000"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SidebarDarkButtonStyle" TargetType="Button" BasedOn="{StaticResource SidebarBaseButtonStyle}">
            <Setter Property="Background" Value="#333333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#111111"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RoundActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.3"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PillButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007CC3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,10,10,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#007CC3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0" VerticalAlignment="Center"/>

                                <Path x:Name="SortArrow" 
                                      Grid.Column="1" 
                                      Margin="8,0,0,0" 
                                      VerticalAlignment="Center" 
                                      Fill="#007CC3" 
                                      Visibility="Collapsed"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="SortDirection" Value="Ascending">
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortArrow" Property="Data" Value="M 0,5 L 5,0 L 10,5 Z"/>
                            </Trigger>
                            <Trigger Property="SortDirection" Value="Descending">
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortArrow" Property="Data" Value="M 0,0 L 5,5 L 10,0 Z"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#EEEEEE"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F8FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#007CC3"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="HorizontalGridLinesBrush" Value="Transparent"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="PillComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#007CC3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border CornerRadius="16" Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 5 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter IsHitTestVisible="False" Margin="10,0,20,0"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              VerticalAlignment="Center" HorizontalAlignment="Center"/>

                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="White" BorderThickness="1" BorderBrush="#DDDDDD" MinWidth="{Binding ActualWidth, ElementName=ToggleButton}" CornerRadius="8" Margin="0,5,0,0">
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PillTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#007CC3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#005A9E"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="16">
                            <Grid>
                                <TextBlock x:Name="PlaceholderText"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="#A0FFFFFF" 
                                           Margin="{TemplateBinding Padding}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>

                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="#007CC3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="Importuj ponownie .csv" Command="{Binding ImportDataCommand}" Style="{StaticResource SidebarBlueButtonStyle}"/>
            <Button Grid.Row="1" Content="Pobierz ponownie bazę" Command="{Binding LoadDataCommand}" Style="{StaticResource SidebarBlueButtonStyle}"/>
            <Button Grid.Row="2" Content="Eksportuj do plików CSV" Command="{Binding ExportDataCommand}" Style="{StaticResource SidebarBlueButtonStyle}"/>

            <Button Grid.Row="3" Content="Generuj raport dla dokumentu" Command="{Binding GenerateReportCommand}" Style="{StaticResource SidebarBlueButtonStyle}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Margin="0,0,0,2">
                            <TextBlock Text="{TemplateBinding Content}" TextWrapping="Wrap" TextAlignment="Center" Padding="10,10"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Grid.Row="5" Content="Wyczyść bazę danych" Command="{Binding ClearDatabaseCommand}" Style="{StaticResource SidebarRedButtonStyle}"/>

            <Button Grid.Row="6" Content="Wróć" Command="{Binding GoBackCommand}" Style="{StaticResource SidebarDarkButtonStyle}" Margin="0,0,0,0"/>
        </Grid>

        <Grid Grid.Column="1" Margin="5,5,5,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1.8*"/>
                <RowDefinition Height="2.5"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Dokumenty" FontSize="14" Margin="0,0,0,5"/>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DataGrid Grid.Row="0" Grid.Column="0" ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument}" 
                          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                          Style="{StaticResource ModernDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" MinWidth="40"/>
                        <DataGridTextColumn Header="Typ" Binding="{Binding Type}" Width="*"/>
                        <DataGridTextColumn Header="Data" Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" Width="*"/>
                        <DataGridTextColumn Header="Imię" Binding="{Binding FirstName}" Width="*"/>
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding LastName}" Width="*"/>
                        <DataGridTextColumn Header="Miasto" Binding="{Binding City}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="&lt;" Command="{Binding PreviousPageCommand}" Style="{StaticResource PillButtonStyle}" Width="30" Height="24" Margin="0,0,10,0"/>

                    <TextBlock Text="Strona " VerticalAlignment="Center" Foreground="#333333" FontSize="12"/>
                    <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" FontWeight="Bold" Foreground="#007CC3" FontSize="12"/>
                    <TextBlock Text=" z " VerticalAlignment="Center" Foreground="#333333" FontSize="12"/>
                    <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center" FontWeight="Bold" Foreground="#333333" FontSize="12"/>

                    <Button Content="&gt;" Command="{Binding NextPageCommand}" Style="{StaticResource PillButtonStyle}" Width="30" Height="24" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Margin="10,0,0,0">

                    <StackPanel Orientation="Horizontal">
                        <Button Background="#008A00" Margin="0,5,10,5" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding AddDocumentCommand}" ToolTip="Dodaj nowy">
                            <iconPacks:PackIconMaterial Kind="Plus" Width="14" Height="14" Margin="0,1,0,0"/>
                        </Button>

                        <ComboBox Width="120" Margin="0,0,0,5" 
                                  Style="{StaticResource PillComboBoxStyle}"
                                  ItemsSource="{Binding FilterColumns}"
                                  SelectedItem="{Binding SelectedFilterColumn, UpdateSourceTrigger=PropertyChanged}">
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Background="#996600" Margin="0,5,10,5" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding EditDocumentCommand}" ToolTip="Edytuj wybrany">
                            <iconPacks:PackIconMaterial Kind="Pencil" Width="14" Height="14"/>
                        </Button>

                        <TextBox Width="120" Margin="0,0,0,0" 
                                 Style="{StaticResource PillTextBoxStyle}"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                 Tag="Szukaj..."/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Background="#990000" Margin="0,5,10,5" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding DeleteDocumentCommand}" ToolTip="Usuń wybrany">
                            <iconPacks:PackIconMaterial Kind="Delete" Width="14" Height="14"/>
                        </Button>
                    </StackPanel>

                </StackPanel>
            </Grid>

            <TextBlock Grid.Row="3" Text="Elementy dokumentu" FontSize="14" Margin="0,0,0,5"/>

            <Grid Grid.Row="4">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedDocument}" Value="{x:Null}">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Setter Property="Opacity" Value="0.4"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DataGrid Grid.Column="0" ItemsSource="{Binding SelectedDocument.Items}" 
                          SelectedItem="{Binding SelectedItem}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          Style="{StaticResource ModernDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Lp." Binding="{Binding Ordinal}" Width="Auto" MinWidth="40"/>
                        <DataGridTextColumn Header="Produkt" Binding="{Binding Product}" Width="2*"/>
                        <DataGridTextColumn Header="Ilość" Binding="{Binding Quantity}" Width="*"/>
                        <DataGridTextColumn Header="Cena" Binding="{Binding Price, StringFormat=\{0:C\}}" Width="*"/>
                        <DataGridTextColumn Header="VAT (%)" Binding="{Binding TaxRate}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Background="#008A00" Margin="0,5,10,5" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding AddItemCommand}" ToolTip="Dodaj nową pozycję">
                            <iconPacks:PackIconMaterial Kind="Plus" Width="14" Height="14" Margin="0,1,0,0"/>
                        </Button>

                        <ComboBox Width="120" Margin="0,0,0,5" 
                                  Style="{StaticResource PillComboBoxStyle}"
                                  ItemsSource="{Binding ItemFilterColumns}"
                                  SelectedItem="{Binding SelectedItemFilterColumn, UpdateSourceTrigger=PropertyChanged}">
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Background="#996600" Margin="0,5,10,5" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding EditItemCommand}" ToolTip="Edytuj wybraną pozycję">
                            <iconPacks:PackIconMaterial Kind="Pencil" Width="14" Height="14"/>
                        </Button>

                        <TextBox Width="120" Margin="0,0,0,0" 
                                 Style="{StaticResource PillTextBoxStyle}"
                                 Text="{Binding ItemSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                 Tag="Szukaj..."/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Background="#990000" Margin="0,5,10,5" Style="{StaticResource RoundActionButtonStyle}" Command="{Binding DeleteItemCommand}" ToolTip="Usuń wybraną pozycję">
                            <iconPacks:PackIconMaterial Kind="Delete" Width="14" Height="14"/>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>