<Window x:Class="ProfiSysTask.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewmodels="clr-namespace:ProfiSysTask.UI.ViewModels"
        xmlns:views="clr-namespace:ProfiSysTask.UI.Views"
        Title="ProfiSys - Task" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:StartViewModel}">
            <views:StartView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:DataViewModel}">
            <views:DataView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>